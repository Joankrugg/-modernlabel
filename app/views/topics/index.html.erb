<%= render 'shared/topic_search' %>
<div class="page-title-search top">
  MUSIC MATCH SALONS
</div>
<div class="banner">
  <div class="banner-content">
    <!--section d'ensemble-->
    <div class="container items">
      <div class="row">
        <% if @topics.present? %>
          <% @topics.each do |a|%>
                <%= render "topic", topic: a %><br>
          <% end %>
        <% else %>
        <div class="no-result" style="color: black">
          sorry! but no result were found!
        </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
