<%= simple_form_for ([ artist, instagram ]) , defaults: { label_html: { class: 'labels' } } do |f| %>
  <% if @instagram.errors.any? %>
    <div id="error_explanation">
      <ul>
      <% @instagram.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="col-xs-12 col-sm-offset-2 col-sm-8 col-md-offset-2 col-md-8 col-lg-offset-1 col-lg-10 form">
    <%= f.input :link, required: false, placeholder: 'https://www.instagram.com/...' %>
    <%= f.button :submit, class: "btn btn-medium-white last-item" %>
  </div>
<% end %>
