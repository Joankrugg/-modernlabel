<%= simple_form_for user, remote: true do |f| %>
  <%= f.error_notification %>
  <%= f.input :email %>
  <%= f.input :city %>
  <%= f.input :description %>
  <%= f.button :submit, class: 'btn btn-medium' %>
<% end %>
