<div class="form-user">
  <%= simple_form_for @user, defaults: { label_html: { class: 'labels' } } do |f| %>
    <% if @user.errors.any? %>
      <div id="error_explanation">
        <ul>
        <% @user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    <div class="col-xs-12 col-sm-offset-2 col-sm-8 col-md-offset-2 col-md-8 col-lg-offset-1 col-lg-10 form">
      <div class="form-group">
        <label class="control-label check_boxes optional">Localisations</label>
        <input type="hidden" name="user[county_ids][]" value="">
        <div class="county-choices">
          <% County.all.each do |county| %>
            <input type="checkbox" class="hidden" value="<%= county.id %>" name="user[county_ids][]" id="user_county_ids_<%= county.id %>">
            <label class="county-choice" for="user_county_ids_<%= county.id %>">
              <%= county.name %>
            </label>
          <% end %>
        </div>
      </div>
      <div class="quality">
        <%= f.button :submit, 'Valider', class: 'btn btn-large-white' %>
        <button type="button" class="btn btn-large-white-off last-item" data-dismiss="modal">Fermer</button>
      </div>
    </div>
  <% end %>
</div>
