<h5 class="negative top">
  SHOP
</h5>
<!--nav network-->
<%= render 'shared/networknav' %>
<div class="container">
  <div class="row">
    <div class="col-xs-6 col-sm-12 col-md-4 col-lg-3">
      <% @shops.each do |s|%>
        <%= render "shop", shop: s %>
      <% end %>
    </div>
  </div>
</div>
